# RFID scope

Direct RFID reader SDK integration is vendor-specific and is not a blocker for Bijoria's barcode-first go-live. The application keeps RFID EPC fields and stock-audit concepts, but a direct hardware reader must receive its own adapter and acceptance test once the exact vendor/model is selected.
