# Windows network profile

All JewelLAN PCs must use a trusted Windows Private network profile. The installer firewall rules intentionally target Private networks. Do not switch the showroom LAN to Public and then weaken firewall rules as a workaround; fix the Windows network profile instead.
