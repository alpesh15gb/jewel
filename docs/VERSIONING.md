# Versioning

JewelLAN uses a human-readable application release label and a numeric Windows file/product version for Inno Setup metadata. Release labels may contain pre-release suffixes; Windows `VersionInfoVersion` and `VersionInfoProductVersion` must remain numeric. Installer artifacts must be traceable to a specific `main` commit and SHA-256 hash before production installation.
