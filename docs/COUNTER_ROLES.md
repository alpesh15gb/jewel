# Counter roles

Create separate named accounts for routine use. Cashiers bill; inventory users manage stock; accounts users review financial reports/Tally; managers handle authorized overrides and returns; administrator access is reserved for system configuration and user administration. The initial administrator account is not a shared counter login.
