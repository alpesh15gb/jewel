# Weighing-scale acceptance

USB-COM scale support is configurable rather than model-specific. Before live use, record the COM port, baud rate/data format and a stable known-weight test. JewelLAN's item-weight validation still applies to values entered or read from the scale. A scale that cannot be read reproducibly must not be relied on for production entry until its adapter/settings are validated.
