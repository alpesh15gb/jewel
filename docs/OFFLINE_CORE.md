# Offline core guarantee

Billing, serialized stock, customers, purchases, accounting, reports, backups and local printing are designed to operate without Internet access. Optional external integrations must never be placed in the synchronous path that commits a JewelLAN sale. Tally synchronization is queued and recoverable rather than a prerequisite for issuing an invoice.
